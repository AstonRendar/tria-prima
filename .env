# El shell de React Native DevTools (Electron) no puede crear su sandbox bajo
# la restricción de user namespaces de Ubuntu 24.04; sin esto, expo start
# registra un error al instalarlo en cada arranque. Solo afecta a esa
# herramienta local de desarrollo.
ELECTRON_DISABLE_SANDBOX=1
